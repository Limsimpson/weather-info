export const SET_MEASURE = 'SET_MEASURE';

export const setMeasure = (measure) => {
    return {
        type: SET_MEASURE,
        measure: measure
    }
}

export const SET_SIDO = 'SET_SIDO';

export const setSido = (sido) => {
    return{
        type: SET_SIDO,
        sido: sido
    }
}